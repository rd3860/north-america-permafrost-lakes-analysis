# Raw Data

This folder contains the smaller datasets used in the time-series and weather analysis.

The full `GLRSED_NA.csv` dataset is not included because it exceeds GitHub's standard file-size limit.
